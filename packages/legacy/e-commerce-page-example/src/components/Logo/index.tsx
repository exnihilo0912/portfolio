import React from 'react';

// === COMPONENT ===
const Logo = (): JSX.Element => {
  return (
    <div className="logo">
      <img src="/assets/images/logo.svg" alt="logo" />
    </div>
  );
};

export default Logo;